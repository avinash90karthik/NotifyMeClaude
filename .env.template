# Silver Hawk Trading - Environment Config
# Copy this to .env and fill in YOUR values

# ── Telegram Bot (create your own via @BotFather - see ONBOARDING.md) ──
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
TELEGRAM_BOT_USERNAME=

# ── Optional: yfinance venv (only if you have a separate venv) ──
# YFINANCE_VENV=/path/to/your/venv/bin/python3

# ── Optional: Chart generation (only if you have the trading-crew scripts) ──
# CHART_SCRIPT=/path/to/generate_chart.py
# CHART_OUTPUT_DIR=/path/to/charts
